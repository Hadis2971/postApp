<!DOCTYPE html>
<html lang="en">
    <head>
        <title>NodeJS Practice</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
        <div id="box" style="border:0.05em solid #bfbfbf;box-sizing:border-box;padding:3%;min-height:25em;">
            {{#each files}}
                <div class="card-body">
                    <img src="/image/{{this.filename}}" class="img-fluid" style="width:100%;">
                    <p class="lead mt-2" style="background-color:#f2f2f2;box-sizing:border-box;padding:1.5%;">{{this.textArea}}</p>
                    <form method="POST" action="/remove/{{this._id}}?_method=DELETE">
                        <button type="submit" class="btn btn-danger mt-3">Delete</button>
                    </form>
                </div>
            {{/each}}    
        </div>
        <form enctype="multipart/form-data" method="POST" class="mt-3" action="/upload">
            <div class="form-group">
                <label for="textArea">What's on your mind...</label>
                <textarea class="form-control" id="textArea" name="textArea" rows="3"></textarea>
            </div>
            <div class="custom-file">
                <input type="file" class="custom-file-input" id="imageFile" name="imageFile">
                <label class="custom-file-label" for="imageFile">Choose file...</label>
                <div class="invalid-feedback">Example invalid custom file feedback</div>
            </div>
            <button class="btn btn-success mt-3">Post</button>
        </form>
    </body>
</html>